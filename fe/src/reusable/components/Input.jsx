export default function Input({ type }) {
  return (
    <input
      type={type}
      className="h-full w-full rounded-md bg-slate-700 p-1 hover:bg-slate-500 focus:bg-slate-500"
    ></input>
  );
}
